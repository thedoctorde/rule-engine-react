
import initialState from './initialState';

export function fieldsReducer(state = initialState.fields, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export function boolParamsReducer(state = initialState.boolParams, action) {
  switch (action.type) {
    default:
      return state;
  }
}
