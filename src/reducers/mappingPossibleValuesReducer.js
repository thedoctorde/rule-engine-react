
import initialState from './initialState';

export default function mappingPossibleValuesReducer(state = initialState.mappingPossibleValues, action) {
  switch (action.type) {
    default:
      return state;
  }
}
