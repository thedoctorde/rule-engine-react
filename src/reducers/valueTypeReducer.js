
import initialState from './initialState';

export function valueTypeReducer(state = initialState.valueTypes, action) {
  switch (action.type) {
    default:
      return state;
  }
}
export function valueTypeForSuberulesReducer(state = initialState.valueTypesForSubrules, action) {
  switch (action.type) {
    default:
      return state;
  }
}

